<h2>tokens</h2>
<table cellspacing='0px'>
	<thead>
	<tr>
		<% for(var attr in Spools.Models.Token.attributes){%>
			<% if(attr == 'id') continue;%>
			<th><%= attr%> </th>	 
		<%}%>
		<th>Options</th>
	</tr>
	</thead>
	<tbody>
		<%= $.View('//spools/views/token/list',{tokens: tokens})%>
	</tbody>
</table>
<h2>New token</h2>
<form>
	<% for(var attr in Spools.Models.Token.attributes){ %>
		<% if(attr == 'id') continue;%>
		<div>
			<label><%= attr %> </label><br />
			<input type="text" value="" name="<%= attr %>"/>
		</div>
	<%}%>
	<input type='submit' value='Create'/>
</form>